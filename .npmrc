; BRAT matches the release tag against manifest.json, which has no "v" prefix.
tag-version-prefix=""

; Commit and annotation message for `npm version`; %s becomes the new version.
message=Bump version to %s
